package ru.silversource.sortingbyinserting.loader;

import static org.junit.Assert.assertEquals;

public class TestGetOpt {

    /*@Test
    public void testCreate(){
        String[] args = "C:\\in-dir\\ --out-prefix=sorted_ --content-type=i --sort-mode=a".split(" ");
        GetOpt opt = null;
        try {
            opt = new GetOpt(args);
        }catch (SortException e){
            e.printStackTrace();
        }
        assertEquals(opt.getPath(), "C:\\in-dir\\");
        assertEquals(opt.getPrefix(), "sorted_");
        assertEquals(opt.getContentType(), "i");
        assertEquals(opt.getSortMode(), "a");

    }*/
}
